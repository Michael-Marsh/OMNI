<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModels="clr-namespace:OMNI.ViewModels" x:Class="OMNI.Views.ReviewImportWindowView"
        mc:Ignorable="d"
        Title="Import Employee Review" Height="150" Width="300" Topmost="True" Icon="/OMNI;component/Icons/Admin.ico" ResizeMode="NoResize">


    <Window.DataContext>
        <ViewModels:ReviewImportViewModel/>
    </Window.DataContext>


    <Grid Style="{DynamicResource GridBackgroundStyle}">

        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <TextBlock Text="{Binding CurrentImport, Mode=OneWay}"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"/>

            <ProgressBar Width="250"
                         Height="35"
                         Value="{Binding Progress, Mode=OneWay}"/>

        </StackPanel>

    </Grid>
</Window>
